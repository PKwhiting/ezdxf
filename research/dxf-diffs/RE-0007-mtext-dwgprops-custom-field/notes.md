# Notes

## Noise Observed

- Not captured yet.

## Open Questions

1. Does a custom DWGPROPS property also use the `AcVar` evaluator family?
2. Does the field code use the `CustomDP.` namespace for arbitrary custom property names?
3. Does the child FIELD payload differ in any meaningful way from the standard Title property case?

## Recommended Follow-Up Experiments

1. Compare the custom property against the standard Title property from `RE-0006`.
2. Repeat the same custom property on `TEXT` once the MTEXT host pattern is confirmed.
